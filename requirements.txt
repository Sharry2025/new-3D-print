# 核心依赖（固定主要版本）
streamlit==1.28.0
pandas==2.0.3
numpy==1.24.3
scikit-learn==1.3.0

# 可视化相关（严格匹配版本）
matplotlib==3.7.1
seaborn==0.12.2
Pillow==10.0.0  # 解决图像处理依赖

# 文本/字体处理
fonttools==4.43.1
nototools==0.2.2  # 思源字体专用工具

# 机器学习工具
scipy==1.10.1
joblib==1.2.0

# 必须固定的间接依赖
markdown-it-py==2.2.0  # 避免与rich冲突
rich==13.7.0
Pygments==2.15.1
mdurl==0.1.2

# 兼容性声明
python-dateutil>=2.8.2
pytz>=2023.3
